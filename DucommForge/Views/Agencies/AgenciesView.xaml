<UserControl x:Class="DucommForge.Views.Agencies.AgenciesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True">
            <TextBlock Text="{Binding Title}"
                       FontSize="22"
                       DockPanel.Dock="Left"
                       VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Refresh"
                        Margin="6,0,0,0"
                        Command="{Binding RefreshCommand}"/>

                <Button Content="{Binding ScopeLabel}"
                        Margin="6,0,0,0"
                        Command="{Binding ToggleScopeCommand}"/>
            </StackPanel>
        </DockPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
            <TextBlock Text="Search:" VerticalAlignment="Center"/>
            <TextBox Width="260"
                     Margin="6,0,0,0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <CheckBox Content="Active only"
                      Margin="12,0,0,0"
                      IsChecked="{Binding ActiveOnly}"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Rows}"
                  SelectedItem="{Binding Selected, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Short" Binding="{Binding Short}" Width="100"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="140"/>

                <DataGridCheckBoxColumn Header="Owned"
                                        Binding="{Binding Owned, Mode=OneWay}"
                                        Width="70"/>

                <DataGridCheckBoxColumn Header="Active"
                                        Binding="{Binding Active, Mode=OneWay}"
                                        Width="70"/>

                <DataGridTextColumn Header="Dispatch Center" Binding="{Binding DispatchCenterCode}" Width="140"/>

                <DataGridTemplateColumn Header="Details" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Details" Command="{Binding DetailsCommand}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Edit" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Edit"
                                    Command="{Binding EditCommand}"
                                    IsEnabled="{Binding CanEdit}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>

</UserControl>